package com.example.composeapp.presentation.theme

import androidx.compose.ui.graphics.Color

// Primary theme colors (soft romantic palette)
val RomanticPink = Color(0xFFFF6F91)     // Main button color
val SoftPink = Color(0xFFFFC0CB)         // Secondary highlights
val LightRose = Color(0xFFFFE4E1)
val HeartColor = Color(0xFFEA605E)// Background for cards/sections

// Backgrounds and surfaces
val CreamWhite = Color(0xFFFFF5F5)       // App background
val PureWhite = Color(0xFFFFFFFF)
val TextColor = Color(0xFF7C615C)// Cards, surfaces

// Text colors
val PrimaryText = Color(0xFF222222)      // Dark text
val SecondaryText = Color(0xFF888888)    // Subtle/muted text
val WhiteText = Color(0xFFFFFFFF)        // For text on colored buttons

// Accent
val HeartRed = Color(0xFFE04E5C)         // Heart icons, accents
